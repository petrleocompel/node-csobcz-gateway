export interface Config {
  gateUrl?: string
  privateKey?: string
  merchantPublicKey?: string
  bankPublicKey?: string
  calbackUrl?: string
  merchantId?: string,
  logging?: any,
  payloadTemplate?: any
}
